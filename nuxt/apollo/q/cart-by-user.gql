query CartByUser($user: ID!) {
  carts(
    where: {
      isCompleted: { equals: false }
      AND: [{ user: { id: { equals: $user } } }]
    }
  ) {
    id
    totalPrice
    items {
      id
      priceWithDiscount
      type
      coupon {
        id
        code
        discount
      }
      course {
        id
        name
        price
      }
      event {
        id
        name
        price
      }
    }
  }
}
